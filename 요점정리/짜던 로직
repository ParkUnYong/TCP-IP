몇가지수를 연산할건지 묻는 것과 숫자를 입력 받아야 하고
숫자가 입력되면 그것이 저장될 배열과 그만큼 숫자 1을 입력하세요 2를 입력하세요 할 반복문 필요

1. 연산할 숫자 전달 들어갈 배열
2. 연산할 숫자 값 전달  들어갈 배열
3. 연산 방법 문자열


char inputnum[BUF_SIZE]
int num[BUF_SIZE];
char cul[BUF_SIZE];

  int num_len = 0;
  fputs("Operand count : ",stdout);
  fgets(inputnum,BUF_SIZE,stdin);
  num_len = atoi(inputnum);
// 서버로 2개 연산 할거라고


  for(int i =0;i<num_len;i++)
  {
    fputs("Operlend i+1 : ",stdout);
    fgets(NUM,BUF_SIZE,stdin);
  }

  fputs("Operator : ",stdout);
  fgets(cul,BUF_SIZE,sizeof(cul));
